{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pip install -r requirements.txt && cd web/frontend && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "/bin/sh -c 'pip install -r requirements.txt && python bot.py & python middleware_listener.py & python -m http.server 8080 --directory web/frontend/dist & wait'"
  }
}